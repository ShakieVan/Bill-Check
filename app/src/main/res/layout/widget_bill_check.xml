<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_trip_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/widget_text"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_rounded_total"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="@color/widget_accent"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_exact_total"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="1dp"
        android:textColor="@color/widget_secondary_text"
        android:textSize="12sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="bottom"
        android:orientation="horizontal"
        android:paddingTop="10dp">

        <TextView
            android:id="@+id/widget_photo"
            style="@style/WidgetAction"
            android:text="@string/widget_photo" />

        <TextView
            android:id="@+id/widget_image"
            style="@style/WidgetAction"
            android:text="@string/widget_image" />

        <TextView
            android:id="@+id/widget_manual"
            style="@style/WidgetAction"
            android:text="@string/widget_manual" />

        <TextView
            android:id="@+id/widget_statement"
            style="@style/WidgetAction"
            android:text="@string/widget_statement" />
    </LinearLayout>
</LinearLayout>
